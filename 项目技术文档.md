# 多模块AI聊天平台 — 项目技术文档

> 版本：v1.0 | 更新日期：2026-05-18 | 作者：sxpsxp

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [项目结构](#3-项目结构)
4. [数据库设计](#4-数据库设计)
5. [后端API接口](#5-后端api接口)
6. [前端架构](#6-前端架构)
7. [核心业务流程](#7-核心业务流程)
8. [部署与运维](#8-部署与运维)
9. [开发规范](#9-开发规范)
10. [变更记录](#10-变更记录)

---

## 1. 项目概述

### 1.1 项目简介

本项目是一个**多模块AI聊天平台**，包含三大子项目：

| 项目 | 说明 | 技术栈 | 端口 |
|------|------|--------|------|
| **spring-boot-admin** | 后端服务（API + 管理后台接口） | Spring Boot 2.7.18 + MyBatis + Redis + MySQL | 8089 |
| **vue-admin** | 管理后台前端 | Vue 2.7 + Element UI + Vuex + Vite | 5173 |
| **ds_ai_web** | AI用户端前端 | Vue 3.5 + Element Plus + Pinia + Vite | 5174 |

### 1.2 核心功能

- **AI对话**：支持多模型（DeepSeek/OpenAI/通义千问/智谱/Moonshot）的SSE流式对话
- **助手模板**：预设角色Prompt，按分类组织，一键切换AI行为模式
- **模型管理**：管理员可配置多AI模型参数、启用/禁用、API密钥管理
- **知识库**：可关联知识库到对话中增强AI回答
- **会话管理**：对话历史保存、搜索、批量删除
- **用户系统**：注册/登录/密码重置、RBAC权限体系、JWT令牌认证
- **系统管理**：用户/角色/菜单/部门/岗位/字典/参数/通知公告管理
- **监控运维**：在线用户、操作日志、登录日志、服务监控、缓存监控
- **代码生成**：基于表结构的CRUD代码自动生成

---

## 2. 技术架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────┐
│                        用户浏览器                              │
│  ┌─────────────────────┐    ┌─────────────────────────────┐  │
│  │   vue-admin (5173)  │    │   ds_ai_web (5174)        │  │
│  │   管理后台前端        │    │   AI用户端前端                 │  │
│  └──────────┬──────────┘    └──────────────┬──────────────┘  │
└─────────────┼──────────────────────────────┼─────────────────┘
              │            HTTP/REST          │
              │     /api/**  /api/ai/**       │
              │     /api/site/**              │
┌─────────────▼──────────────────────────────▼─────────────────┐
│                  spring-boot-admin (8089)                      │
│  ┌───────────┐ ┌───────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ Controller│ │  Service  │ │  Mapper  │ │   Security   │  │
│  │  24个      │ │  18个     │ │  20个    │ │   JWT+RBAC   │  │
│  └───────────┘ └───────────┘ └──────────┘ └──────────────┘  │
└──────────────────────────────────────────────────────────────┘
              │                    │
       ┌──────▼──────┐    ┌───────▼───────┐
       │   MySQL 8.0 │    │   Redis 7.x   │
       │   主数据库    │    │   缓存+会话    │
       └─────────────┘    └───────────────┘
```

### 2.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | 应用框架 |
| Spring Security | 5.7.x | 安全认证与授权 |
| Spring AOP | 2.7.18 | 操作日志切面 |
| MyBatis | 2.3.2 | ORM框架 |
| MySQL Connector | 8.0.33 | 数据库驱动 |
| Redis (Lettuce) | - | 缓存与会话存储 |
| OkHttp | 4.12.0 | AI模型API调用 |
| Knife4j (Swagger) | 4.3.0 | API文档 |
| Jackson | 2.13.x | JSON序列化 |

### 2.3 前端技术栈

**管理后台 (vue-admin)**：
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 2.7.16 | 前端框架 |
| Vue Router | 3.6.5 | 路由管理 |
| Vuex | 3.6.2 | 状态管理 |
| Element UI | 2.15.14 | UI组件库 |
| Axios | 1.7.2 | HTTP客户端 |
| Vite | 5.4.19 | 构建工具 |

**AI用户端 (ds_ai_web)**：
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.5.13 | 前端框架（Composition API） |
| Vue Router | 4.5.0 | 路由管理 |
| Pinia | 2.3.0 | 状态管理 |
| Element Plus | 2.9.1 | UI组件库（中文） |
| Axios | 1.7.9 | HTTP客户端 |
| @vueuse/core | 11.3.0 | 组合式工具库 |
| marked | 14.1.4 | Markdown渲染 |
| highlight.js | 11.11.1 | 代码语法高亮 |
| KaTeX | 0.16.47 | 数学公式渲染 |
| Vite | 5.4.19 | 构建工具 |

---

## 3. 项目结构

### 3.1 后端项目结构 (spring-boot-admin)

```
spring-boot-admin/
├── pom.xml                                    # Maven配置
├── src/main/java/com/example/admin/
│   ├── AdminApplication.java                  # 应用入口
│   ├── aspect/
│   │   └── LogAspect.java                     # 操作日志AOP切面
│   ├── config/
│   │   ├── SecurityConfig.java                # Spring Security配置
│   │   ├── CorsConfig.java                    # 跨域配置
│   │   ├── SwaggerConfig.java                 # Knife4j文档配置
│   │   ├── GlobalExceptionHandler.java        # 全局异常处理
│   │   ├── TokenAuthenticationFilter.java     # JWT令牌认证过滤器
│   │   ├── LoginRateLimitFilter.java          # 登录速率限制
│   │   ├── XssFilter.java                     # XSS防护
│   │   └── PasswordMigrationRunner.java       # 密码迁移
│   ├── controller/                            # 24个RestController
│   │   ├── LoginController.java               # 登录/注册/用户信息
│   │   ├── UserController.java                # 用户管理
│   │   ├── RoleController.java                # 角色管理
│   │   ├── MenuController.java                # 菜单管理
│   │   ├── DeptController.java                # 部门管理
│   │   ├── PostController.java                # 岗位管理
│   │   ├── DictTypeController.java            # 字典类型
│   │   ├── DictDataController.java            # 字典数据
│   │   ├── ConfigController.java              # 参数配置
│   │   ├── NoticeController.java              # 通知公告
│   │   ├── OperLogController.java             # 操作日志
│   │   ├── LoginLogController.java            # 登录日志
│   │   ├── OnlineUserController.java          # 在线用户
│   │   ├── JobController.java                 # 定时任务
│   │   ├── MonitorController.java             # 服务监控
│   │   ├── GenController.java                 # 代码生成
│   │   ├── DashboardController.java           # 控制台统计
│   │   ├── AiChatController.java              # AI聊天(SSE)
│   │   ├── AiConversationController.java      # AI对话CRUD
│   │   ├── AiMessageController.java           # AI消息查询
│   │   ├── AiConfigController.java            # AI用户配置
│   │   ├── AiModelController.java             # AI模型列表(公开)
│   │   ├── AiKnowledgeBaseController.java     # AI知识库列表(公开)
│   │   ├── AiAssistantPublicController.java   # AI助手模板(公开)
│   │   ├── SiteConfigPublicController.java    # 站点配置(公开)
│   │   ├── AiModelAdminController.java        # 管理后台-模型管理
│   │   ├── AiKnowledgeBaseAdminController.java # 管理后台-知识库管理
│   │   ├── AiChatRecordAdminController.java   # 管理后台-聊天记录
│   │   ├── AiAssistantAdminController.java    # 管理后台-助手模板
│   │   ├── AiAssistantCategoryAdminController.java # 管理后台-助手分类
│   │   └── SiteConfigAdminController.java     # 管理后台-站点配置
│   ├── entity/                                # 20个实体类
│   │   ├── User.java, Role.java, Menu.java, Dept.java, Post.java
│   │   ├── DictType.java, DictData.java, Config.java, Notice.java
│   │   ├── OperLog.java, LoginLog.java, Job.java
│   │   ├── AiConversation.java, AiMessage.java, AiModel.java
│   │   ├── AiKnowledgeBase.java, AiConfig.java
│   │   ├── SysSiteConfig.java, AiAssistant.java, AiAssistantCategory.java
│   │   └── Result.java                        # 统一响应封装
│   ├── mapper/                                # 20个Mapper接口
│   ├── service/                               # 18个Service + model/子包
│   │   ├── TokenService.java                  # JWT令牌管理
│   │   ├── UserDetailsServiceImpl.java        # Spring Security用户加载
│   │   ├── AiChatService.java                 # AI聊天核心编排
│   │   ├── DeepSeekClient.java                # DeepSeek OkHttp客户端
│   │   ├── AiMessageService.java              # 消息服务（Redis缓存+MySQL）
│   │   ├── AiConversationService.java         # 对话服务（Redis缓存+MySQL）
│   │   ├── StreamSessionManager.java          # SSE流会话管理
│   │   └── model/                             # AI模型提供者策略模式
│   │       ├── ModelProvider.java             # 接口定义
│   │       ├── AbstractOpenAIProvider.java    # OpenAI兼容抽象实现
│   │       ├── ModelProviderFactory.java      # 工厂路由
│   │       ├── DeepSeekProvider.java          # DeepSeek提供者
│   │       ├── OpenAIProvider.java            # OpenAI提供者
│   │       ├── QwenProvider.java              # 通义千问提供者
│   │       ├── ZhipuProvider.java             # 智谱提供者
│   │       └── MoonshotProvider.java          # Moonshot提供者
│   └── store/
│       └── DataStore.java                     # 内存数据存储
├── src/main/resources/
│   ├── application.yml                        # 主配置文件
│   ├── schema.sql                             # 数据库建表脚本
│   ├── data.sql                               # 初始数据脚本
│   └── mapper/                                # 20个MyBatis XML映射文件
```

### 3.2 管理后台前端结构 (vue-admin)

```
vue-admin/
├── package.json
├── vite.config.js                             # Vite配置（代理/api到8089）
├── src/
│   ├── main.js                                # 入口：注册Element UI/Vuex/Router
│   ├── App.vue                                # 根组件
│   ├── permission.js                          # 路由守卫 + 动态路由生成
│   ├── router/index.js                        # 路由配置（dynamicRouteMap）
│   ├── store/index.js                         # Vuex状态管理
│   ├── utils/
│   │   └── request.js                         # Axios实例(baseURL: /api)
│   ├── api/                                   # 23个API模块
│   │   ├── login.js, user.js, role.js, menu.js
│   │   ├── dept.js, post.js, dict.js, config.js, notice.js
│   │   ├── dashboard.js, operlog.js, loginlog.js, online.js
│   │   ├── job.js, server.js, cache.js, gen.js
│   │   ├── aiModel.js, aiChatRecord.js, aiKnowledgeBase.js
│   │   ├── assistant.js, assistantCategory.js, siteConfig.js
│   ├── layout/                                # 布局组件
│   │   ├── index.vue                          # 主布局（侧边栏+顶栏+内容区）
│   │   ├── Navbar.vue                         # 顶栏（面包屑+用户下拉）
│   │   ├── Sidebar.vue                        # 侧边菜单栏
│   │   └── MenuItem.vue                       # 递归菜单项
│   └── views/                                 # 25个页面
│       ├── login/                             # 登录页
│       ├── dashboard/                         # 控制台首页
│       ├── system/                            # 系统管理(7个页面)
│       │   ├── user/, role/, menu/, dept/, post/
│       │   ├── dict/, config/, notice/, site-config/
│       ├── monitor/                           # 监控管理(6个页面)
│       │   ├── operlog/, loginlog/, online/
│       │   ├── job/, server/, cache/, druid/
│       ├── tool/                              # 系统工具(3个页面)
│       │   ├── gen/, swagger/, build/
│       └── ai/                                # AI管理(5个页面)
│           ├── model/, chat-record/, knowledge/
│           ├── assistant-category/, assistant/
```

### 3.3 AI用户端前端结构 (ds_ai_web)

```
ds_ai_web/
├── package.json
├── vite.config.js                             # Vite配置（代理/api到8089）
├── src/
│   ├── main.js                                # 入口：注册Pinia/Router/Element Plus
│   ├── App.vue                                # 根组件（暗色模式切换）
│   ├── router/index.js                        # 5条路由（/, /login, /register, /forgot-password, /profile）
│   ├── utils/
│   │   ├── constants.js                       # 默认参数常量
│   │   └── format.js                          # 格式化工具
│   ├── api/                                   # 9个API模块
│   │   ├── request.js                         # Axios实例(baseURL: /api/ai)
│   │   ├── site.js                            # Axios实例(baseURL: /api/site)
│   │   ├── auth.js, chat.js, conversation.js
│   │   ├── message.js, model.js
│   │   ├── knowledge.js, assistant.js, config.js
│   ├── stores/                                # 8个Pinia Store
│   │   ├── theme.js, user.js, chat.js
│   │   ├── conversation.js, model.js, config.js
│   │   ├── knowledge.js, assistant.js
│   ├── composables/                           # 4个组合式函数
│   │   ├── useSseChat.js                      # SSE流式聊天逻辑
│   │   ├── useAutoScroll.js                   # 消息列表自动滚动
│   │   ├── useMobile.js                       # 响应式断点检测
│   │   └── useMarkdown.js                     # Markdown渲染（marked+highlight+KaTeX）
│   ├── styles/                                # 5个样式文件
│   │   ├── variables.scss                     # CSS变量
│   │   ├── global.scss                        # 全局样式
│   │   ├── markdown.scss                      # Markdown渲染样式
│   │   └── element-overrides.scss             # Element Plus覆写
│   ├── components/
│   │   ├── common/                            # 通用组件(3个)
│   │   │   ├── HeaderToolbar.vue              # 顶部工具栏
│   │   │   ├── ThemeToggle.vue                # 暗色/亮色切换
│   │   │   └── CodeCopyButton.vue             # 代码复制按钮
│   │   ├── layout/                            # 布局组件(3个)
│   │   │   ├── AppLayout.vue                  # 三列网格主布局
│   │   │   ├── LeftSidebar.vue                # 左侧对话列表面板
│   │   │   └── RightPanel.vue                 # 右侧设置面板
│   │   ├── chat/                              # 聊天组件(5个)
│   │   │   ├── ChatView.vue                   # 聊天主视图（含快速导航）
│   │   │   ├── ChatMessage.vue                # 消息气泡
│   │   │   ├── ChatInput.vue                  # 输入框
│   │   │   ├── MarkdownRenderer.vue           # Markdown渲染组件
│   │   │   └── EmptyState.vue                 # 欢迎页
│   │   ├── conversation/                      # 对话列表组件(3个)
│   │   │   ├── ConversationList.vue           # 对话列表容器
│   │   │   ├── ConversationItem.vue           # 单条对话
│   │   │   └── ConversationSearch.vue         # 搜索框
│   │   └── settings/                          # 设置面板组件(6个)
│   │       ├── ModelSelector.vue              # 模型选择器
│   │       ├── AssistantSelector.vue          # 助手模板选择器
│   │       ├── KnowledgeSelector.vue          # 知识库选择器
│   │       ├── TemperatureSlider.vue          # 温度参数滑块
│   │       ├── MaxTokensSlider.vue            # 最大Tokens滑块
│   │       └── SystemPromptEditor.vue         # 系统提示词编辑
│   └── views/                                 # 4个独立页面
│       ├── Login.vue, Register.vue
│       ├── ForgotPassword.vue, Profile.vue
```

---

## 4. 数据库设计

### 4.1 数据库信息

- **数据库名**：`ai_test`
- **字符集**：utf8mb4
- **引擎**：InnoDB
- **表总数**：20张（系统表9张 + 字典表2张 + 日志表2张 + AI表7张 + 任务/站点/助手表5张）

### 4.2 核心系统表

#### sys_user（用户表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password | VARCHAR(100) | BCrypt加密密码 |
| nickname | VARCHAR(50) | 昵称 |
| email | VARCHAR(100) | 邮箱 |
| phone | VARCHAR(20) | 手机号 |
| avatar | VARCHAR(200) | 头像URL |
| status | INT | 0=正常 1=停用 |
| create_time | DATETIME | 创建时间 |

#### sys_role（角色表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| role_key | VARCHAR(50) UNIQUE | 角色标识符 |
| role_name | VARCHAR(50) | 角色名称 |
| status | INT | 0=正常 1=停用 |

#### sys_menu（菜单表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| parent_id | BIGINT | 父菜单ID |
| name | VARCHAR(50) | 菜单名称 |
| path | VARCHAR(200) | 路由路径 |
| component | VARCHAR(200) | 组件路径 |
| icon | VARCHAR(50) | 图标 |
| perms | VARCHAR(100) | 权限标识 |
| menu_type | CHAR(1) | M=目录 C=菜单 F=按钮 |
| sort | INT | 排序号 |
| visible | INT | 0=显示 1=隐藏 |
| status | INT | 0=正常 1=停用 |

### 4.3 AI业务表

#### ai_conversation（AI对话表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| title | VARCHAR(200) | 对话标题 |
| user_id | BIGINT NOT NULL | 所属用户ID |
| model_id | VARCHAR(50) | 使用的模型ID |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间（自动更新） |
| 索引 | idx_conv_user (user_id) | |

#### ai_message（AI消息表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| conversation_id | BIGINT NOT NULL | 所属对话ID |
| role | VARCHAR(20) | user/assistant/system |
| content | TEXT NOT NULL | 消息内容 |
| create_time | DATETIME | 创建时间 |
| 索引 | idx_msg_conv (conversation_id) | |

#### ai_model（AI模型表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| model_key | VARCHAR(50) | API模型标识（如deepseek-chat） |
| name | VARCHAR(100) | 模型显示名称 |
| provider | VARCHAR(50) | 提供商标识（deepseek/openai/qwen/zhipu/moonshot） |
| capabilities | VARCHAR(200) | 能力标签（逗号分隔：chat,code,reasoning） |
| api_base_url | VARCHAR(300) | API基础URL |
| api_key | VARCHAR(200) | 模型专属API密钥 |
| sort | INT | 排序号 |
| status | INT | 0=启用 1=禁用 |

#### ai_knowledge_base（AI知识库表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| name | VARCHAR(100) | 知识库名称 |
| description | VARCHAR(500) | 描述 |
| status | INT | 0=启用 1=禁用 |

#### ai_config（AI用户配置表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| user_id | BIGINT UNIQUE | 用户ID（一对一） |
| temperature | DOUBLE | 温度参数（默认0.7） |
| max_tokens | INT | 最大Tokens（默认2048） |
| system_prompt | VARCHAR(2000) | 系统提示词 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间（自动更新） |

#### ai_assistant_category（助手分类表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| name | VARCHAR(50) | 分类名称 |
| sort | INT | 排序号 |
| status | INT | 0=启用 1=禁用 |

#### ai_assistant（助手模板表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| category_id | BIGINT NOT NULL | 所属分类ID |
| name | VARCHAR(100) | 助手名称 |
| description | VARCHAR(500) | 描述 |
| icon | VARCHAR(50) | Element Plus图标名 |
| prompt | TEXT NOT NULL | 系统Prompt预设内容 |
| sort | INT | 排序号 |
| status | INT | 0=启用 1=禁用 |

#### sys_site_config（站点配置表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键 |
| config_key | VARCHAR(100) UNIQUE | 配置键 |
| config_value | VARCHAR(500) | 配置值 |
| config_name | VARCHAR(100) | 配置名称 |
| remark | VARCHAR(200) | 备注 |

### 4.4 关联表

- **sys_user_role**：用户-角色多对多关联
- **sys_role_menu**：角色-菜单多对多关联

---

## 5. 后端API接口

### 5.1 认证接口（公开）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/login` | 用户登录（返回token） |
| POST | `/api/register` | 用户注册 |
| POST | `/api/send-code` | 发送短信验证码 |
| POST | `/api/verify-identity` | 验证身份 |
| POST | `/api/reset-password` | 重置密码 |
| POST | `/api/logout` | 登出（需认证） |
| GET | `/api/user/info` | 获取当前用户信息（需认证） |
| GET | `/api/menus/routers` | 获取动态路由菜单（需认证） |
| PUT | `/api/user/profile` | 更新个人资料（需认证） |

### 5.2 系统管理接口（需认证+权限）

**用户管理** `/api/users`：
| 方法 | 路径 | 权限 |
|------|------|------|
| GET | `/api/users` | system:user:list |
| GET | `/api/users/{id}` | system:user:list |
| POST | `/api/users` | system:user:create |
| PUT | `/api/users` | system:user:edit |
| DELETE | `/api/users/{id}` | system:user:delete |

**角色管理** `/api/roles`、**菜单管理** `/api/menus`、**部门管理** `/api/depts`、**岗位管理** `/api/posts`、**字典管理** `/api/dict-types`+`/api/dict-data`、**参数配置** `/api/configs`、**通知公告** `/api/notices`——均提供完整CRUD接口。

**日志与监控**：
| 路径 | 说明 |
|------|------|
| `/api/oper-logs` | 操作日志查询/删除/清空 |
| `/api/login-logs` | 登录日志查询/删除/清空 |
| `/api/online-users` | 在线用户列表/强制下线 |
| `/api/monitor/server` | 服务器监控信息 |
| `/api/monitor/cache` | 缓存监控信息 |
| `/api/dashboard` | 控制台统计数据 |

### 5.3 AI接口

**用户端接口**（`/api/ai`）：

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/ai/chat/send` | 需认证 | SSE流式聊天 |
| POST | `/api/ai/chat/stop` | 需认证 | 停止当前流 |
| GET | `/api/ai/chat/status` | 需认证 | 查询流状态 |
| GET | `/api/ai/conversations` | 需认证 | 对话列表 |
| GET | `/api/ai/conversations/{id}` | 需认证 | 对话详情 |
| POST | `/api/ai/conversations` | 需认证 | 创建对话 |
| PUT | `/api/ai/conversations` | 需认证 | 更新对话 |
| DELETE | `/api/ai/conversations/{id}` | 需认证 | 删除对话 |
| DELETE | `/api/ai/conversations/batch` | 需认证 | 批量删除对话 |
| GET | `/api/ai/conversations/{id}/messages` | 需认证 | 对话消息列表 |
| DELETE | `/api/ai/messages/{id}` | 需认证 | 删除单条消息 |
| GET | `/api/ai/config` | 需认证 | 获取用户AI配置 |
| PUT | `/api/ai/config` | 需认证 | 保存用户AI配置 |
| GET | `/api/ai/models` | 公开 | 可用AI模型列表 |
| GET | `/api/ai/knowledge-bases` | 公开 | 可用知识库列表 |
| GET | `/api/ai/assistants` | 公开 | 助手模板列表（按分类分组） |

**管理后台接口**（`/api/admin`，需管理员权限）：

| 路径 | 说明 |
|------|------|
| `/api/admin/models` | AI模型CRUD + 启用/禁用 |
| `/api/admin/knowledge-bases` | AI知识库CRUD |
| `/api/admin/chat-records` | 对话记录分页查询/消息查看/删除 |
| `/api/admin/assistants` | 助手模板CRUD |
| `/api/admin/assistant-categories` | 助手分类CRUD |
| `/api/admin/site-configs` | 站点配置查看/修改 |

**站点接口**（`/api/site`，公开）：

| 路径 | 说明 |
|------|------|
| GET `/api/site/config` | 获取站点配置（名称/Logo/版权等） |

### 5.4 统一响应格式

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { }
}
```

- `code=200` 表示成功，其他值表示错误
- `msg` 为操作提示信息
- `data` 为业务数据

### 5.5 SSE流式响应格式

聊天接口 `POST /api/ai/chat/send` 返回 `text/event-stream`：

```
data:{"content":"你好"}
data:{"reasoning_content":"思考过程"}
data:{"done":true}
data:{"error":"错误信息"}
```

---

## 6. 前端架构

### 6.1 管理后台 (vue-admin)

**认证与路由流程**：
1. 用户访问任意页面 → `router.beforeEach` 路由守卫触发
2. 无token → 跳转 `/login`
3. 有token但无用户信息 → 调用 `GET /user/info` + `GET /menus/routers` 获取用户信息和菜单
4. 遍历菜单树，通过 `dynamicRouteMap` 查找组件，调用 `router.addRoutes()` 动态添加路由
5. 用户看到 Layout 布局（侧边菜单 + 顶栏 + 内容区）

**状态管理**（单一Vuex Store）：
- `token`：JWT认证令牌（持久化到localStorage）
- `user`：当前用户信息
- `roles`：角色列表
- `permissions`：权限标识列表
- `menus`：动态菜单树

**Axios拦截器**：
- 请求拦截：自动附加 `Authorization: Bearer {token}`
- 响应拦截：`code=401` 跳转登录页；`code!=200` 显示错误消息

### 6.2 AI用户端 (ds_ai_web)

**响应式三列布局**：
- **桌面端**（>=1200px）：左栏（对话列表280px）+ 中间（聊天1fr）+ 右栏（设置面板320px）
- **平板端**（768-1199px）：左栏 + 聊天 + 右栏抽屉
- **移动端**（<=767px）：左栏抽屉 + 聊天

**Pinia Store架构**（8个模块）：
| Store | 职责 |
|-------|------|
| `chat` | 消息列表、流式状态、StreamContent管理 |
| `conversation` | 对话列表、搜索、选中、批量操作 |
| `model` | 模型列表、选中模型、能力标签 |
| `config` | 温度/Tokens/系统提示词、800ms防抖保存 |
| `knowledge` | 知识库列表、多选 |
| `assistant` | 助手分类、分组列表、选中助手 |
| `user` | 用户信息、登录/注册/登出 |
| `theme` | 暗色/亮色模式切换 |

**SSE流式聊天流程**（`useSseChat.js`）：
1. 验证token有效性
2. 无对话则先创建对话
3. 添加用户消息到本地状态
4. 通过 `fetch` POST发送到 `/api/ai/chat/send`
5. 读取 `ReadableStream`，逐行解析SSE `data:` 事件
6. 实时更新 `currentStreamContent` / `currentReasoningContent`
7. 接收 `done` 信号后 `finalizeStream` 写入消息列表
8. 支持通过 `AbortController` 中断流

**Markdown渲染**（`useMarkdown.js`）：
- 基于 `marked` 解析Markdown
- `highlight.js` 代码语法高亮（支持GitHub主题）
- `KaTeX` 渲染行内和块级数学公式
- 代码块自动注入复制按钮

---

## 7. 核心业务流程

### 7.1 AI模型提供者策略模式

```
AiChatService
  └─> ModelProviderFactory.getProvider(provider)
        ├─ "deepseek"  → DeepSeekProvider
        ├─ "openai"    → OpenAIProvider
        ├─ "qwen"      → QwenProvider
        ├─ "zhipu"     → ZhipuProvider
        └─ "moonshot"  → MoonshotProvider
              └─> 所有继承 AbstractOpenAIProvider
                    └─> 通过 OkHttp 调用 OpenAI 兼容格式 API
```

每个Provider从配置文件读取 `api-key` 和 `base-url`，支持环境变量注入敏感信息。

### 7.2 Redis缓存策略

**消息列表缓存**（`AiMessageService`）：
- 缓存键：`ds-ai:messages:conversation:{conversationId}`
- **无TTL**，靠写操作主动失效
- 读：缓存命中直接返回JSON → 反序列化
- 读：缓存未命中 → 查MySQL → 写入Redis
- 写（save）：写入MySQL → 从MySQL重新加载最新列表 → 更新Redis缓存
- 写（delete）：查找归属对话 → 删除MySQL记录 → 重建对应的Redis缓存
- 写（deleteByConversationId）：删除MySQL中所有消息 → 删除Redis缓存

**对话列表缓存**（`AiConversationService`）：
- 列表缓存键：`ds-ai:conversations:user:{userId}`（无TTL）
- 单项缓存键：`ds-ai:conversation:{id}`（TTL 60分钟）
- 创建/更新/删除时：重建列表缓存，删除单项缓存
- 确保服务重启后从MySQL回源，缓存逐步预热

### 7.3 认证与授权流程

1. **登录**：用户提交用户名+密码 → BCrypt验证 → 生成UUID token → 存入 `DataStore.tokenMap` → 返回token
2. **令牌验证**：`TokenAuthenticationFilter` 从 `Authorization: Bearer` 标头提取token → 查询 `DataStore.tokenMap` → 设置 `SecurityContextHolder`
3. **权限验证**：`@PreAuthorize("hasAuthority('system:user:list')")` 从角色-菜单关联中解析权限字符串
4. **登出**：删除 `DataStore.tokenMap` 中对应的token → 清空前端localStorage

### 7.4 动态菜单路由

1. 后端 `sys_menu` 表存储菜单树（含 `component` 路径、`perms` 权限标识）
2. 前端 `dynamicRouteMap` 映射 `component路径` → `懒加载Vue组件`
3. 登录后调用 `GET /menus/routers` 获取用户的菜单树
4. 遍历菜单树，匹配组件并调用 `router.addRoutes()` 注入路由
5. 侧边栏根据菜单树渲染

---

## 8. 部署与运维

### 8.1 环境要求

| 组件 | 版本要求 |
|------|---------|
| JDK | 1.8+ |
| Maven | 3.6+ |
| Node.js | 16+ |
| MySQL | 5.7+ / 8.0+ |
| Redis | 5.0+ |

### 8.2 数据库初始化

1. 创建MySQL数据库：`CREATE DATABASE IF NOT EXISTS ai_test DEFAULT CHARSET utf8mb4;`
2. 应用启动时自动执行 `schema.sql`（建表）和 `data.sql`（初始数据）
3. 配置项 `spring.sql.init.mode=always` 确保每次启动都检查表结构
4. 配置项 `spring.sql.init.continue-on-error=true` 确保重复执行不报错

### 8.3 启动服务

**方式一：命令行启动**

```bash
# 1. 启动后端
cd spring-boot-admin
mvn spring-boot:run

# 2. 启动管理后台
cd vue-admin
npm install
npm run dev

# 3. 启动AI用户端
cd ds_ai_web
npm install
npm run dev
```

**方式二：使用启动脚本（参见项目根目录脚本）**

### 8.4 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 超级管理员 | admin | admin123 |
| 普通用户 | user | user123 |

### 8.5 访问地址

| 服务 | 地址 |
|------|------|
| AI用户端 | http://localhost:5174 |
| 管理后台 | http://localhost:5173 |
| API文档 | http://localhost:8089/doc.html |
| 后端API | http://localhost:8089/api |

### 8.6 配置文件关键项

**application.yml**：
```yaml
server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_test?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  redis:
    host: localhost
    port: 6379

# AI模型API密钥配置
deepseek:
  api-key: ${DEEPSEEK_API_KEY:sk-xxx}
  base-url: https://api.deepseek.com

openai:
  api-key: ${OPENAI_API_KEY:}
  base-url: https://api.openai.com

qwen:
  api-key: ${QWEN_API_KEY:}
  base-url: https://dashscope.aliyuncs.com/compatible-mode

zhipu:
  api-key: ${ZHIPU_API_KEY:}
  base-url: https://open.bigmodel.cn/api/paas

moonshot:
  api-key: ${MOONSHOT_API_KEY:}
  base-url: https://api.moonshot.cn
```

### 8.7 安全配置

- **公开路径**：登录/注册/密码重置、Swagger文档、AI模型/知识库/助手列表、站点配置
- **需认证**：所有其他 `/api/**` 路径
- **速率限制**：同一IP+用户每分钟最多5次登录尝试，封禁15分钟
- **XSS防护**：请求参数和请求头XSS过滤
- **CORS**：允许跨域访问

---

## 9. 开发规范

### 9.1 代码规范

- **语言**：注释和文档使用简体中文，代码和标识符使用英文
- **命名**：类名PascalCase、方法/变量camelCase、常量UPPER_SNAKE_CASE、表/字段小写下划线
- **格式**：4空格缩进、单行长度不超过120字符、K&R大括号风格
- **异常处理**：捕获具体异常类型，禁止吞掉Exception，必须记录日志

### 9.2 数据库规范

- 表名/字段名：小写 + 下划线分隔
- 主键：bigint unsigned、自增、非空
- 索引命名：`idx_字段`（普通索引）、`uniq_字段`（唯一索引）
- 禁止 SELECT *，必须明确字段列表
- 禁止存储过程、外键、触发器

### 9.3 工程规范

- **分层**：Controller → Service → Mapper → DB
- **POJO隔离**：DO（实体）/ DTO（传输）/ VO（视图）严格区分
- **配置文件**：使用 `application.yml` 统一管理
- **依赖注入**：使用 `@Autowired` 字段注入

### 9.4 Git规范

- **主分支**：`main`（生产就绪）
- **开发分支**：`dev`（日常开发）
- **提交信息**：简洁明了，描述变更内容

---

## 10. 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-05-18 | v1.0 | 初始文档：涵盖项目架构、数据库设计、API接口、部署运维 |
| 2026-05-18 | v1.0 | AI聊天导航按钮：增加跳转到问题开始/结束功能 |
| 2026-05-18 | v1.0 | Redis缓存优化：消息/对话列表缓存改为无TTL+写失效模式 |
| 2026-05-18 | v1.0 | 助手模板功能完善：分类分组展示、卡片式选择器 |
| 2026-05-18 | v1.0 | AI模型管理：多模型提供者策略模式、状态启用/禁用 |
| 2026-05-18 | v1.0 | 管理后台AI模块：模型管理、聊天记录、知识库、助手管理、站点配置 |

---

> 本文档随项目迭代持续更新。如有疑问，请联系项目负责人。
