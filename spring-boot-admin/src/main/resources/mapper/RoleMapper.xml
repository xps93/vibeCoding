<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.RoleMapper">

    <resultMap id="roleMap" type="Role">
        <id property="id" column="id"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleName" column="role_name"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="roleMap">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="roleMap">
        SELECT * FROM sys_role
        <where>
            <if test="keyword != null and keyword != ''">
                AND (role_name LIKE CONCAT('%', #{keyword}, '%') OR role_key LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role(role_key, role_name, status, create_time)
        VALUES(#{roleKey}, #{roleName}, #{status}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_role
        SET role_key = #{roleKey},
            role_name = #{roleName},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

    <select id="selectMenuIdsByRoleId" resultType="long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <insert id="insertRoleMenu">
        INSERT INTO sys_role_menu(role_id, menu_id) VALUES(#{roleId}, #{menuId})
    </insert>

    <delete id="deleteRoleMenuByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

</mapper>
