<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.NoticeMapper">

    <resultMap id="noticeMap" type="Notice">
        <id property="id" column="id"/>
        <result property="noticeTitle" column="notice_title"/>
        <result property="noticeType" column="notice_type"/>
        <result property="noticeContent" column="notice_content"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="noticeMap">
        SELECT * FROM sys_notice WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="noticeMap">
        SELECT * FROM sys_notice
        <where>
            <if test="keyword != null and keyword != ''">
                AND notice_title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_notice(notice_title, notice_type, notice_content, status, create_time)
        VALUES(#{noticeTitle}, #{noticeType}, #{noticeContent}, #{status}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_notice
        SET notice_title = #{noticeTitle},
            notice_type = #{noticeType},
            notice_content = #{noticeContent},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_notice WHERE id = #{id}
    </delete>

</mapper>
