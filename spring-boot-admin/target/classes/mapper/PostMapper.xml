<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.PostMapper">

    <resultMap id="postMap" type="Post">
        <id property="id" column="id"/>
        <result property="postCode" column="post_code"/>
        <result property="postName" column="post_name"/>
        <result property="postSort" column="post_sort"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="postMap">
        SELECT * FROM sys_post WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="postMap">
        SELECT * FROM sys_post
        <where>
            <if test="keyword != null and keyword != ''">
                AND (post_name LIKE CONCAT('%', #{keyword}, '%') OR post_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY post_sort
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_post(post_code, post_name, post_sort, status, create_time)
        VALUES(#{postCode}, #{postName}, #{postSort}, #{status}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_post
        SET post_code = #{postCode},
            post_name = #{postName},
            post_sort = #{postSort},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_post WHERE id = #{id}
    </delete>

</mapper>
