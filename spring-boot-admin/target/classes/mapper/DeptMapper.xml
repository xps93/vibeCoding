<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.DeptMapper">

    <resultMap id="deptMap" type="Dept">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="deptName" column="dept_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="leader" column="leader"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="deptMap">
        SELECT * FROM sys_dept WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="deptMap">
        SELECT * FROM sys_dept
        <where>
            <if test="keyword != null and keyword != ''">
                AND dept_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY order_num
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_dept(parent_id, dept_name, order_num, leader, phone, email, status, create_time)
        VALUES(#{parentId}, #{deptName}, #{orderNum}, #{leader}, #{phone}, #{email}, #{status}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_dept
        SET parent_id = #{parentId},
            dept_name = #{deptName},
            order_num = #{orderNum},
            leader = #{leader},
            phone = #{phone},
            email = #{email},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_dept WHERE id = #{id}
    </delete>

</mapper>
